spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/angia
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:slam7424}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:Y29uZ190eV90bmhoX3RtZHZfYW5fZ2lhXzIwMjVfc2VjcmV0}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
  upload:
    dir: s:/angia_website/angia-backend/uploads

logging:
  level:
    vn.dichvuangia: DEBUG
    org.springframework.security: DEBUG
  file:
    name: s:/management/app-dev.log


